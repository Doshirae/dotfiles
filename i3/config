# Colours ==>
set_from_resource $background i3wm.background #121212
set_from_resource $black i3wm.color0      #5E5E5E
set_from_resource $red i3wm.color1        #6F694D
set_from_resource $green i3wm.color2      #749253
set_from_resource $yellow i3wm.color3     #B2BB50
set_from_resource $blue i3wm.color4       #607B8C
set_from_resource $purple i3wm.color5     #607B8C
set_from_resource $cyan i3wm.color6       #669972
set_from_resource $grey i3wm.color7       #393939

#                Border        Background   Font          ???
client.focused   $red          $background  $foreground   #000000
client.unfocused $background   $background  $foreground   #000000
client.urgent    $background   $red         $background   #000000
# colours <==

# Other ==>
font pango:Cantarell 9
floating_modifier $mod
workspace_auto_back_and_forth no
# set_from_resource $bw i3wm.bw 4
set $bw 0
new_window pixel $bw
new_float pixel $bw
# <==

# Sets ==>
set $mod Mod4
set $Locker ~/.dotfiles/scripts/lock.sh
set $browser firefox
set $file thunar
set $mail claws-mail

set $WS1 1
set $WS2 2
set $WS3 3
set $WS4 4
set $WS5 5
set $WS6 6
set $WS7 7
set $WS8 8
set $WS9 9
set $WS0 10
# sets <==

# Bindsyms ==>
# Actions ==>
bindsym $mod+x kill
# Screenshots ==>
bindsym $mod+s exec .dotfiles/scripts/screenshot.sh full
bindsym $mod+Shift+s exec .dotfiles/scripts/screenshot.sh area
bindsym $mod+Mod1+s exec .dotfiles/scripts/screenshot.sh window
# screenshots <==
# Audio & Brightness ==>
bindsym XF86MonBrightnessDown exec xbacklight -dec 5
bindsym XF86MonBrightnessUp exec xbacklight -inc 5
bindsym XF86AudioRaiseVolume exec "amixer -q set Master 5%+"
bindsym XF86AudioLowerVolume exec "amixer -q set Master 5%-"
bindsym XF86AudioMute exec "amixer -q sset Master toggle"
# audio brightness <==
# actions <==
# Applications ==>
bindsym $mod+Return exec urxvtc; workspace $WS2
bindsym $mod+d exec --no-startup-id rofi -show run
bindsym $mod+F1 exec $browser
bindsym $mod+F2 exec urxvtc; workspace $WS2
bindsym $mod+F3 exec thunar
# bindsym $mod+F4 exec urxvtc -title "ranger" -e zsh -c "ranger"; workspace $WS3
bindsym $mod+F4 exec liferea; workspace $WS4
bindsym $mod+F5 exec $mail; workspace $WS5
bindsym $mod+F6 exec transmission-gtk; workspace $WS6
# bindsym $mod+x exec xkill
bindsym $mod+n exec passmenu
bindsym $mod+w exec urxvtc -geometry 60x40 -title "nmtui" -e zsh -c "sudo nmtui"
# <==
# Movement ==>
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
# layout <==
# Layout ==>
bindsym $mod+c split horizontal
bindsym $mod+v split vertical
bindsym $mod+f fullscreen toggle
bindsym $mod+e layout toggle tabbed split
bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle
# bindsym $mod+Shift+s sticky toggle
bindsym $mod+p focus parent
bindsym $mod+o focus child
# layout <==
# Workspaces ==>
bindsym $mod+1 workspace $WS1
bindsym $mod+2 workspace $WS2
bindsym $mod+3 workspace $WS3
bindsym $mod+4 workspace $WS4
bindsym $mod+5 workspace $WS5
bindsym $mod+6 workspace $WS6
bindsym $mod+7 workspace $WS7
bindsym $mod+8 workspace $WS8
bindsym $mod+9 workspace $WS9
bindsym $mod+twosuperior workspace $WS0

bindsym $mod+Shift+1 move container to workspace $WS1
bindsym $mod+Shift+2 move container to workspace $WS2
bindsym $mod+Shift+3 move container to workspace $WS3
bindsym $mod+Shift+4 move container to workspace $WS4
bindsym $mod+Shift+5 move container to workspace $WS5
bindsym $mod+Shift+6 move container to workspace $WS6
bindsym $mod+Shift+7 move container to workspace $WS7
bindsym $mod+Shift+8 move container to workspace $WS8
bindsym $mod+Shift+9 move container to workspace $WS9
bindsym $mod+Shift+twosuperior move container to workspace $WS0

bindsym $mod+Ctrl+Right workspace next
bindsym $mod+Ctrl+Left workspace prev
# workspaces <==
# i3 ==>
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart

# bindsym $mod+g mode "$gaps"
# i3 <==
# bindsyms <==

# Assigns ==>
assign [class="Chromium|Firefox|qutebrowser"] → $WS1
assign [title="urxvt|tmux"] → $WS2
assign [class="Thunar|Filezilla"] → $WS3
assign [class="Liferea"] → $WS4
assign [class="Geary"] → $WS5
assign [class="Transmission"] → $WS6
# assigns <==

# For_windows ==>
for_window [title="alsamixer"] floating enable border pixel 2
for_window [class="*pinentry*"] floating enable
for_window [title="*pinentry*"] floating enable
for_window [title="nmtui"] floating enable
for_window [title="File Transfer*"] floating enable
for_window [class="GParted"] floating enable border normal
for_window [class="Lightdm-gtk-greeter-settings"] floating enable
for_window [class="Lxappearance"] floating enable sticky enable border normal
for_window [class="Pavucontrol"] floating enable
for_window [title="TK-*"] floating enable

for_window [urgent=latest] focus
for_window [class=".*"] title_format "     %title"
# for_windows <==

# Modes ==>
# system ==>
bindsym $mod+i mode "$system"
set $system Lock | Exit | Suspend | Hibernate | Reboot | Shutdown
mode "$system" {
	bindsym l exec --no-startup-id $Locker, mode "default"
		bindsym e exec --no-startup-id i3-msg exit, mode "default"
		bindsym s exec --no-startup-id $Locker && systemctl suspend, mode "default"
		bindsym h exec --no-startup-id $Locker && systemctl hibernate, mode "default"
		bindsym r exec --no-startup-id reboot, mode "default"
		bindsym Shift+s exec --no-startup-id poweroff, mode "default"

		bindsym Escape mode "default"
}
# system <==
# resize ==>
bindsym $mod+r mode "resize"
mode "resize" {
	bindsym h resize shrink width 5 px or 5 ppt
	bindsym j resize grow height 5 px or 5 ppt
	bindsym k resize shrink height 5 px or 5 ppt
	bindsym l resize grow width 5 px or 5 ppt

	bindsym Left resize shrink width 5 px or 5 ppt
	bindsym Down resize grow height 5 px or 5 ppt
	bindsym Up resize shrink height 5 px or 5 ppt
	bindsym Right resize grow width 5 px or 5 ppt

	bindsym Escape mode "default"
	bindsym $mod+r mode "default"
}
# resize <==
# browser ==>
bindsym $mod+b mode "$browser_mode"
set $browser_mode Firefox | Chromium | Qutebrowser | Surf
mode "$browser_mode" {
	bindsym f exec firefox, mode "default"
		bindsym c exec chromium --incognito, mode "default"
		bindsym q exec qutebrowser, mode "default"
		bindsym s exec surf, mode "default"

		bindsym Escape mode "default"
}
# browser <==
# mouse ==>
bindsym $mod+m mode "$mouse_big_mode"
set $mouse_big_mode big | h - j - k - l | w - x - c - J - K | s
set $mouse_small_mode small | h - j - k - l | w - x - c - J - K | b

# h, j, k, l are for moving (Vim like)
# w, x, c, v, b are for Left click, middle click, right click, wheel up, wheel down
# q, s are for bigger movements and smaller movements

# big ==>
mode "$mouse_big_mode" {
	bindsym h exec xdotool mousemove_relative -- -100 0
	bindsym j exec xdotool mousemove_relative -- 0 100
	bindsym k exec xdotool mousemove_relative -- 0 -100
	bindsym l exec xdotool mousemove_relative -- 100 0

	bindsym w exec xdotool click 1
	bindsym Shift+w exec xdotool mousedown 1
	bindsym Shift+x exec xdotool mouseup 1
	bindsym x exec xdotool click 2
	bindsym c exec xdotool click 3
	bindsym a exec xdotool click 4
	bindsym z exec xdotool click 5

	bindsym s mode "$mouse_small_mode"

	bindsym Escape mode "default"
	bindsym $mod+m mode "default"
}
# big <==
# small ==>
mode "$mouse_small_mode" {
	bindsym h exec xdotool mousemove_relative -- -10 0
	bindsym j exec xdotool mousemove_relative -- 0 10
	bindsym k exec xdotool mousemove_relative -- 0 -10
	bindsym l exec xdotool mousemove_relative -- 10 0

	bindsym w exec xdotool click 1
	bindsym Shift+w exec xdotool mousedown 1
	bindsym Shift+x exec xdotool mouseup 1
	bindsym x exec xdotool click 2
	bindsym c exec xdotool click 3
	bindsym a exec xdotool click 4
	bindsym z exec xdotool click 5

	bindsym b mode "$mouse_big_mode"

	bindsym Escape mode "default"
	bindsym $mod+m mode "default"
}
# small <==
# mouse <==
# play/pause ==>
# set $playpause Play/Pause
# bindsym $mod+less mode "playpause"
# mode "playpause" {
# 	bindsym k exec "xdotool mousemove 38 663 click 1 mousemove restore", mode "default"
#
# 	bindsym Escape mode "default"
# 	bindsym $mod+less mode "default"
# }
# <==
# modes <==

# Autostart ==>
exec_always ~/.dotfiles/scripts/wallpaper.sh
exec_always xrdb ~/.Xresources
exec_always "killall polybar; polybar -r top"

exec sleep 2 && ~/.dotfiles/scripts/dunst.sh
exec sleep 3 && ~/.dotfiles/scripts/update.sh
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec nm-applet
exec pamac-tray
exec mpd
exec compton -b
exec urxvtd -q -o -f
exec setxkbmap fr oss -option compose:menu

exec --no-startup-id gnome-keyring-daemon
# exec --no-startup-id xautolock -time 10 -locker $Locker
# autostart <==

# i3-gaps ==>
gaps inner 0
smart_gaps on
smart_borders on
# <==

# vim:ft=i3
